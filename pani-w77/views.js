'use strict';
function stationSectorPage(){let k=sectorKey();return `<div class="card"><div class="k">${STATION_ID} // SETOR AUTORIZADO</div><h2>${N[k]}</h2><p class="mut">A estação não utiliza mais a antiga matriz de contingência orbital neste terminal. Funções, arquivos e restauração de acesso são agora vinculados ao setor ${NL[k]}.</p><div class="nominal"><b class="ok">✓ CREDENCIAL SETORIAL VALIDADA</b><p class="mut small">Use PORTAS ENTERRADAS para reconstruir o acesso assinado por A. Dena.</p></div><div class="actions" style="margin-top:14px"><button class="btn a" onclick="view='sepulcro';render(true)">ABRIR PORTAS ENTERRADAS</button><button class="btn" onclick="view='files';render(true)">ARQUIVOS DO SETOR</button></div></div>`}
function pani(){return `<div class="card"><div class="k">ASSISTÊNCIA OPERACIONAL // ${STATION_ID}</div><h2>PANI</h2><p class="mut">Consultas incompatíveis com sua credencial podem ser registradas sem resposta. A PANI reconhece setores, portas e arquivos; ela não traduz automaticamente os Sinais.</p><input id="q" placeholder="Pergunte à PANI"><button id="ask" class="btn a">ENVIAR</button><div id="ans" class="term" style="margin-top:14px">PANI aguardando consulta.</div></div>`}
function files(){return `<div class="card"><h2>ARQUIVOS AUTORIZADOS</h2><p class="mut">Sincronizando arquivo central...</p></div>`}
function creds(){return `<div class="card"><h2>CREDENCIAIS</h2><div class="term">${(prog.credentials||[]).map(esc).join('\n')||'Nenhuma credencial adicional registrada.'}</div><input id="cr" placeholder="CÓDIGO"><button id="crs" class="btn a">REGISTRAR</button></div>`}
